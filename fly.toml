# fly.toml app configuration file generated for aegisforge-bot on 2026-05-12T01:54:27Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'aegisforge-bot'
primary_region = 'ams'

[build]

[env]
  PORT = '8080'
  RUST_LOG = 'aegisforge=info,sqlx=warn'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = 'off'
  auto_start_machines = true
  min_machines_running = 1
  processes = ['app']

  [http_service.concurrency]
    type = 'requests'
    hard_limit = 250
    soft_limit = 200

  [[http_service.checks]]
    interval = '30s'
    timeout = '5s'
    grace_period = '10s'
    method = 'GET'
    path = '/'

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 1024
